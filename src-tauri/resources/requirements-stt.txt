# Python dependencies for the PA-owned venv at ~/.pocket-agent/venv/.
# Auto-installed by venv::ensure_venv() on first launch.

# numpy<2 is required: torch / ctranslate2 wheels (pulled in by faster-whisper)
# are built against the NumPy 1.x C ABI and crash at import on NumPy 2.x.
numpy<2.0
faster-whisper>=1.0.0
silero-vad>=5.1
onnxruntime>=1.17
edge-tts>=6.1
