# Python dependencies for stt-server.py
# Install into the venv used to launch the server (default: ~/.hermes/hermes-agent/venv).
#
#   ~/.hermes/hermes-agent/venv/bin/pip install -r src-tauri/resources/requirements-stt.txt
#
# Override the python interpreter with STT_PYTHON if your venv lives elsewhere.

# numpy<2 is required: torch / ctranslate2 wheels (pulled in by faster-whisper)
# are built against the NumPy 1.x C ABI and crash at import on NumPy 2.x.
numpy<2.0
faster-whisper>=1.0.0
silero-vad>=5.1
onnxruntime>=1.17
